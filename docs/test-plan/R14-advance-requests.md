# R14 · หัวหน้าโครงการตั้งเบิกค่าแรงให้ลูกน้อง + เจ้าของอนุมัติ — ตารางตรวจรับ

> คำสั่งเจ้าของ 21 ก.ย. 2569: *"พี่เพิ่มให้หัวหน้างานส่งตั้งเบิกค่าแรงให้ลูกน้องคนอื่นได้ไหมครับ
> แล้วค่อยให้ผมอนุมัติ … เบิกเกินได้ครับ เดี๋ยวผมจะอนุมัติอีกทีให้เบิกหรือไม่เบิก
> แค่ให้เค้าส่งเสนอมาก่อน บางครั้งผมไม่ได้อยู่หน้างาน เค้าจะแจ้งหัวหน้างานมาว่าขอเบิกเงิน"*

**สถานะ:** `☐` ยังไม่ตรวจ · `✅` ผ่านโดยมีหลักฐาน · `❌` ตก · `👤` ต้องคนตรวจ · `⚠️` ติดเงื่อนไข

> ✅ **apply migration บนฐานจริงแล้ว 22 ก.ย. 2569** (โปรเจ็ค `kdftrlagqovjsgejahwz`)
> · `generate_typescript_types` แล้ว **diff กับไฟล์ที่เขียนมือว่างเปล่า** (`R14-DB-00`)
> · ใบเบิกเดิม 16 ใบกลายเป็น `approved` ครบ · ยอดคงเหลือของทั้ง 12 คน **เท่าเดิมทุกบาท**
> (เทียบสูตรก่อน/หลังแบบตัวต่อตัว) · `get_advisors` security/performance = **ERROR 0** ทั้งคู่
>
> 🧪 **`node scripts/verify-advance-db.mjs` — 29/29 ผ่านบนฐานจริง 22 ก.ย. 2569**
> ทุกอย่างอยู่ใน `do` block ที่จบด้วย `raise exception` → **rollback ทั้งก้อน**
> (fixture · ใบเบิก · รอบจ่าย · แจ้งเตือน · `audit_log` หายพร้อมกัน) จึงรันบนฐานของลูกค้า
> ได้โดยไม่ต้องมีโค้ดเก็บกวาดที่อาจลบผิดแถว (§17 ข้อ 9 · 14 · 24 · 28 ล้วนมาจากโค้ดเก็บกวาด)
> · ตรวจของค้างหลังรัน = 0 ทุกตาราง รวม `audit_log`
> · สวมสิทธิ์ด้วย `set local role authenticated` + `request.jwt.claims` จึงทดสอบ **RLS จริง**
> ไม่ใช่ทดสอบในฐานะ superuser ที่ข้าม RLS ไปทั้งหมด
>
> 🧪 ฝั่ง API: `node scripts/verify-advance-requests.mjs` (ต่อเข้า `verify:all` แล้ว)
> · **ยังไม่เคยรัน** — ต้องมีบัญชี `SEED_*` + dev server · ฐานจริงมีผู้ใช้จริง 5 คน
> ไม่มีบัญชีเดโม่ให้ล็อกอิน · รันบนฐานพัฒนาที่ seed แล้วเท่านั้น

## 0 · พื้นผิวที่แตะ

| นับอะไร | จากไหน | จำนวน |
|---|---|---|
| enum ใหม่ | `advance_status` (`pending`/`approved`/`rejected`) · ค่าใหม่ของ `notification_kind` 3 ค่า | **1 + 3** |
| คอลัมน์ใหม่บน `advances` | `status` `rejected_reason` `approved_by` `approved_at` | **4** |
| ฟังก์ชันที่ต้องกรอง `status='approved'` | `employee_balance` · `payroll_balances` · `close_payroll_run` · `report_summary` · `report_labor` · `mcp_create_advance` (เขียน `approved`) | **6** |
| policy ใหม่/แก้ | `advances` select · insert · update · delete | **4** |
| trigger | `guard_advance` (แก้) · `notify_advance` (ใหม่) | **2** |
| endpoint | `POST /api/advances` (รับหัวหน้าโครงการ) · `PATCH /api/advances/[id]` (ใหม่) · `DELETE /api/advances/[id]` (ขยายถึงผู้ขอ) | **3** |
| หน้าใหม่ | `/advances` (หัวหน้าโครงการ) | **1** |
| หน้าแก้ | `/approvals` (คิวคำขอเบิก) · `/payroll` (นับเฉพาะที่อนุมัติแล้ว) · เมนู + ป้ายตัวเลข | **3** |

## 1 · R14-DB — กติกาที่ฐานข้อมูลบังคับ

| ID | Trigger | ผลที่คาด | สถานะ |
|----|---------|----------|--------|
| R14-DB-00 | apply migration แล้ว `generate_typescript_types` | diff กับไฟล์ที่เขียนมือว่าง | ✅ diff ว่าง |
| R14-DB-01 | ใบเบิกเก่าทั้งหมดหลัง migration | `status = 'approved'` ทุกแถว (ไม่มีเงินที่จ่ายไปแล้วกลายเป็น "รออนุมัติ") | ✅ 16/16 ใบ |
| R14-DB-02 | ค่าตั้งต้นของคอลัมน์หลัง migration | `default 'pending'` — แถวที่ไม่มีใครระบุสถานะต้องไม่กลายเป็นเงินที่จ่ายแล้ว | ✅ |
| R14-DB-03 | หัวหน้าโครงการ insert พร้อม `status='approved'` | `APPROVE_FORBIDDEN` (guard ทับค่าไม่ได้ ต้องปฏิเสธให้เห็น) | ✅ |
| R14-DB-04 | หัวหน้าโครงการ insert โดยไม่ใส่ `site_id` (**เขียนใหม่ 22 ก.ย. 2569** — เดิมบังคับ `SITE_REQUIRED`) | ผ่าน · `status='pending'` · `site_id` เป็น null — ค่าแรงเป็นของคน ไม่ใช่ของโครงการ | ✅ |
| R14-DB-05 | หัวหน้าโครงการ insert `site_id` ของโครงการที่ไม่ได้ดูแล | RLS ปฏิเสธ (0 แถว) | ✅ |
| R14-DB-06 | หัวหน้าโครงการ insert ปกติ | แถวเกิดด้วย `status='pending'` · `created_by` = ตัวเอง แม้ส่ง `created_by` อื่นมา | ✅ |
| R14-DB-07 | หัวหน้าโครงการ `update status='approved'` ใบของตัวเอง | `APPROVE_FORBIDDEN` | ✅ |
| R14-DB-08 | หัวหน้าโครงการแก้ยอดใบที่ถูกตีกลับของตัวเอง | สถานะเด้งกลับ `pending` เอง · `rejected_reason` ถูกล้าง (= ส่งใหม่) | ✅ |
| R14-DB-09 | หัวหน้าโครงการแก้ใบที่ `approved` แล้ว | RLS ปฏิเสธ (0 แถว) | ✅ |
| R14-DB-10 | เจ้าของ `update status='rejected'` โดยไม่ใส่เหตุผล | check `advances_rejected_needs_reason` ปฏิเสธ | ✅ |
| R14-DB-11 | เจ้าของอนุมัติ | `approved_by` = เจ้าของ · `approved_at` = now() (trigger เติม ไม่ใช่ client ส่ง) | ✅ |
| R14-DB-12 | อนุมัติใบเดิมซ้ำ (`status` ไม่เปลี่ยน) | ไม่มีแจ้งเตือนใบที่สอง | ✅ |
| R14-DB-13 | ใบที่ `deducted_amount > 0` ถูก `update status` | `PAYROLL_CLOSED` — เงินหักไปแล้ว ถอนอนุมัติไม่ได้ | ✅ |
| R14-DB-14 | ใบ `pending` ลงวันในอนาคต | `DATE_FUTURE` (กฎเดิมยังบังคับครบ) | ✅ |
| R14-DB-15 | ใบ `pending` ของคน A ยอด ฿5,000 | `employee_balance(A).advanced` **ไม่เปลี่ยน** — คำขอไม่ใช่เงินที่จ่ายไปแล้ว | ✅ |
| R14-DB-16 | คน A มีค่าแรงค้าง ฿3,000 + ใบ `pending` ฿5,000 แล้วกดจ่ายค่าแรง | จ่ายจริง ฿3,000 · `advance_deducted = 0` · ใบ `pending` ยังค้างอยู่ ไม่ถูกตีตราว่าหักแล้ว | ✅ |
| R14-DB-17 | เหมือน R14-DB-16 แต่อนุมัติใบก่อนจ่าย | หักได้ ฿3,000 · ใบเหลือค้าง ฿2,000 (`deducted_amount = 3000`) ไปรอบหน้า | ✅ |
| R14-DB-18 | `report_summary` / `report_labor` ช่วงที่มีใบ `pending` | `advance_paid` ไม่รวมใบนั้น | ✅ |
| R14-DB-19 | `mcp_create_advance` (AI คีย์แทนเจ้าของ) | ได้ `status='approved'` ทันที ไม่ตกค้างในคิว | ✅ |
| R14-DB-20 | หัวหน้าโครงการ select ใบเบิกของหัวหน้าคนอื่น | 0 แถว (เห็นเฉพาะที่ตัวเองยื่น) | ✅ |
| R14-DB-21 | หัวหน้าโครงการเรียก `employee_balance()` ตรง | `FORBIDDEN` — ค่าแรงเป็นความลับจากหัวหน้าโครงการ (P4.5) | ✅ |
| R14-DB-22 | `get_advisors` (security + performance) | ไม่มีรายการใหม่ · มี index `advances(status) where status='pending'` | ✅ ERROR 0 |
| R14-DB-24 | **เจ้าของ insert โดยไม่ส่ง `status`** (โค้ดเวอร์ชันก่อน R14 ที่ยัง deploy อยู่) | `approved` เสมอ + `approved_by` เติมให้ — เงินที่จ่ายออกไปแล้วห้ามกลายเป็น "คำขอ" | ✅ |
| R14-DB-24b | หัวหน้าโครงการ insert โดยไม่ส่ง `status` | ยังเป็น `pending` เหมือนเดิม (คู่ตรงข้ามของ R14-DB-24) | ✅ |
| R14-DB-25 | คนที่มีหนี้ ฿2,000 โดยไม่มีค่าแรงเลย | `overdrawn_employees()` คืน `balance = −2000` · `overdrawn_summary()` = 1 คน ฿2,000 (สองตัวตรงกัน) | ✅ |
| R14-DB-26 | **ลงชื่อเข้าโครงการได้ค่าแรง ฿500** | ยอดติดลบเหลือ −฿1,500 **ทันที** · `advances.deducted_amount` ยัง **0** — ค่าแรงวันนั้นต้องไม่ถูกนับสองรอบ | ✅ |
| R14-DB-27 | กดจ่ายค่าแรงหลังจากนั้น | หักคืนจริง ฿500 · `audit_log` มีแถว UPDATE ของใบเบิกที่ `deducted_amount` เปลี่ยน | ✅ |
| R14-DB-28 | หัวหน้าโครงการเรียก `overdrawn_employees()` | **0 แถว** (ไม่ใช่เลขศูนย์ที่อ่านเหมือนไม่มีหนี้) — ค่าแรงเป็นความลับจากเขา | ✅ |
| R14-DB-23 | migration ซ้ำ | ไม่มี error (enum/คอลัมน์/policy idempotent) | ⚠️ ไม่รันซ้ำบนฐานจริงโดยตั้งใจ — idempotent ด้วย `if not exists` ทุกคำสั่ง แต่ยืนยันได้เฉพาะบนฐานพัฒนา |

## 2 · R14-API

> ☐ ทั้งบล็อกนี้ยังไม่ถูกตรวจ — `verify-advance-requests.mjs` เขียนไว้ครบแล้วแต่ต้องมี
> บัญชี `SEED_*` กับ dev server · ฐานจริงไม่มีบัญชีเดโม่ให้ล็อกอิน
> · กฎที่ route พึ่งพา (สถานะตาม role · ขอบเขตโครงการ · ใบที่ถูกหักแล้ว) ถูกตรวจที่
> ชั้นฐานข้อมูลครบแล้วใน §1 ซึ่งเป็นด่านจริง — ที่ยังขาดคือรหัสสถานะ HTTP และข้อความไทย

| ID | Trigger | ผลที่คาด | สถานะ |
|----|---------|----------|--------|
| R14-API-01 | หัวหน้าโครงการ `POST /api/advances` ครบฟิลด์ | 201 · `status: 'pending'` · **ไม่มี `balance` ในคำตอบ** (ความลับค่าแรง) | ☐ |
| R14-API-02 | หัวหน้าโครงการ POST โดยไม่ส่ง `siteId` (**เขียนใหม่ 22 ก.ย. 2569**) | **201** · `status: 'pending'` · ฟอร์มไม่มีช่องโครงการให้เลือกอีกแล้ว | ☐ |
| R14-API-03 | หัวหน้าโครงการ POST `siteId` ที่ไม่ได้ดูแล | 403 `FORBIDDEN` | ☐ |
| R14-API-04 | เจ้าของ POST | 201 · `status: 'approved'` · คืน `balance` + `overdrawn` เหมือนเดิม | ☐ |
| R14-API-05 | เจ้าของ `PATCH` `{action:'approve'}` | 200 · แถวเป็น `approved` | ☐ |
| R14-API-06 | เจ้าของ `PATCH` `{action:'reject'}` ไม่มีเหตุผล | 400 `REASON_REQUIRED` | ☐ |
| R14-API-07 | หัวหน้าโครงการ `PATCH` `{action:'approve'}` | 403 `FORBIDDEN` | ☐ |
| R14-API-08 | `PATCH` action บนใบที่ `approved` แล้ว | 409 `ALREADY_APPROVED` | ☐ |
| R14-API-09 | ผู้ขอ `PATCH` แก้ยอดใบ `pending` ของตัวเอง | 200 · ยอดเปลี่ยน · ยังเป็น `pending` | ☐ |
| R14-API-10 | ผู้ขอ `PATCH` แก้ใบที่ `approved` | 403 `EDIT_FORBIDDEN` | ☐ |
| R14-API-11 | ผู้ขอ `DELETE` ใบ `pending`/`rejected` ของตัวเอง | 200 · แถวหาย | ☐ |
| R14-API-12 | ผู้ขอ `DELETE` ใบของคนอื่น | 403/404 | ☐ |
| R14-API-13 | `DELETE` ใบที่ `deducted_amount > 0` | 409 `PAYROLL_CLOSED` (กฎเดิม) | ☐ |
| R14-API-14 | POST ด้วยปี พ.ศ. (`2569-…`) | 400 `DATE_BUDDHIST_ERA` | ☐ |

## 3 · R14-UI — หน้าของหัวหน้าโครงการ (`/advances`)

| ID | Trigger | ผลที่คาด | สถานะ |
|----|---------|----------|--------|
| R14-UI-01 | หัวหน้าโครงการเปิด `/advances` | เห็นปุ่ม "ตั้งเบิกให้ลูกน้อง" + รายการคำขอของตัวเอง | 👤 |
| R14-UI-02 | ยังไม่เคยยื่นคำขอ | สถานะว่าง (ไม่ใช่ตารางเปล่า) พร้อมบอกว่าทำอะไรต่อ | 👤 |
| R14-UI-03 | ทุกหน้าจอของคำขอ | **ไม่มียอดค่าแรง/ยอดคงเหลือของคนงานปรากฏที่ไหนเลย** | 👤 |
| R14-UI-04 | ยื่นคำขอสำเร็จ | toast ไทย + แถวขึ้นสถานะ "รออนุมัติ" ทันที | 👤 |
| R14-UI-05 | คำขอถูกตีกลับ | เห็นเหตุผลบนแถว + ปุ่ม "แก้แล้วส่งใหม่" และ "ลบ" ที่ทำงานจริง (§17 ข้อ 18) | 👤 |
| R14-UI-06 | คำขอได้รับอนุมัติ | สถานะ "อนุมัติแล้ว" · ไม่มีปุ่มแก้/ลบ (ตรงกับ policy) | 👤 |
| R14-UI-07 | เจ้าของเปิด `/advances` | เด้งไป `/payroll` (ช่องทางของเจ้าของอยู่ที่นั่น) | 👤 |
| R14-UI-08 | เมนู | หัวหน้าโครงการเห็น "ตั้งเบิกค่าแรง" · เจ้าของไม่เห็น | 👤 |
| R14-UI-09 | ป้ายตัวเลขเมนู | ของค้างของหัวหน้าโครงการ = คำขอที่ถูกตีกลับ · 0 = ไม่วาดป้าย | 👤 |
| R14-UI-10 | 4 สถานะของหน้า | โครงร่าง / ผิดพลาด+ลองใหม่ / ว่าง / สำเร็จ ครบ | 👤 |
| R14-UI-11 | 390 / 768 / 1440 · สว่าง+มืด | ไม่มีอะไรล้นขอบ · ปุ่มย้อนกลับอยู่แถวเดียวกับหัวข้อ (`PageHeader`) | 👤 |

## 4 · R14-APR — คิวอนุมัติของเจ้าของ (`/approvals`)

| ID | Trigger | ผลที่คาด | สถานะ |
|----|---------|----------|--------|
| R14-APR-01 | มีคำขอเบิกค้าง | โผล่เป็นกล่องของตัวเองเหนือคิวรายจ่าย เรียงเก่าก่อน | 👤 |
| R14-APR-02 | แต่ละแถว | ชื่อคนงาน · ยอด · วันที่ · โครงการ · ใครยื่น · ค้างกี่วัน | 👤 |
| R14-APR-03 | คนงานที่ค่าแรงค้างจ่ายน้อยกว่ายอดที่ขอ | ป้ายเตือน "เกินค่าแรงค้างจ่าย ฿X" **แต่ปุ่มอนุมัติยังกดได้** (เบิกเกินได้) | 👤 |
| R14-APR-04 | กดอนุมัติ | แถวหายจากคิว · ยอด "เบิกไปแล้ว" ใน `/payroll` เพิ่มขึ้นเท่ายอดนั้น | 👤 |
| R14-APR-05 | กดตีกลับโดยไม่พิมพ์เหตุผล | ปุ่มยืนยันกดไม่ได้ + ข้อความบอกว่าต้องมีเหตุผล | 👤 |
| R14-APR-06 | กดตีกลับพร้อมเหตุผล | แถวหายจากคิว · ผู้ยื่นได้รับแจ้งเตือนพร้อมเหตุผลนั้น | 👤 |
| R14-APR-07 | ป้ายตัวเลขเมนู "รออนุมัติ" | = รายจ่ายค้าง + คำขอเบิกค้าง (นับในฐานข้อมูลทั้งคู่) | 👤 |
| R14-APR-08 | ไม่มีของค้างทั้งสองชนิด | สถานะว่างเดิมยังถูกต้อง | 👤 |

## 5 · R14-NOTIF

| ID | Trigger | ผลที่คาด | สถานะ |
|----|---------|----------|--------|
| R14-NOTIF-01 | หัวหน้าโครงการยื่นคำขอ | เจ้าของทุกคนที่ `is_active` ได้แจ้งเตือน `advance_pending` ลิงก์ `/approvals` | ✅ |
| R14-NOTIF-02 | เจ้าของอนุมัติ | ผู้ยื่นได้ `advance_approved` ลิงก์ `/advances` | ⚠️ ตรวจพร้อม R14-API-05 บนฐานพัฒนา |
| R14-NOTIF-03 | เจ้าของตีกลับ | ผู้ยื่นได้ `advance_rejected` **พร้อมเหตุผลในตัวข้อความ** | ✅ |
| R14-NOTIF-04 | เจ้าของคีย์เบิกเอง (`approved` ตั้งแต่แรก) | ไม่มีแจ้งเตือนถึงใคร | ⚠️ ตรวจพร้อม R14-API-04 บนฐานพัฒนา |
| R14-NOTIF-05 | คำขอที่ถูกตีกลับถูกแก้แล้วส่งใหม่ | เจ้าของได้แจ้งเตือนว่ามีของกลับเข้าคิว | ⚠️ ตรวจพร้อม R14-DB-08 รอบหน้า — trigger เขียนไว้แล้ว |
| R14-NOTIF-06 | แถวแจ้งเตือนใหม่ | ถูก push ตามคิวเดิม (`pushed_at` null → cron) โดยไม่ต้องแก้ตัวส่ง | ⚠️ ต้องมี push subscription จริง |

## 6 · R14-FIX — รอบแก้จากการใช้จริง (22 ก.ย. 2569)

> เจ้าของแจ้งสองเรื่องหลังลูกค้าใช้จริงวันแรก:
> *"เมื่อหัวหน้างานเบิกเงิน มันให้เลือกโครงการ ทั้ง ๆ ที่ถ้าเจ้าของเบิกเองมันไม่มีให้เลือก"*
> และ *"อยากให้เบิกเกินได้ ต้องมีลอจิกเก็บยอดติดลบของคนงานแต่ละคน"*

| ID | Trigger | ผลที่คาด | สถานะ |
|----|---------|----------|--------|
| R14-FIX-01 | ฟอร์มยื่นคำขอของหัวหน้าโครงการ | **ไม่มีช่อง "โครงการ" อีกแล้ว** — เหลือ คนงาน · ยอด · วันที่ · หมายเหตุ | 👤 |
| R14-FIX-02 | ยื่นคำขอโดยไม่ผูกโครงการ | 201 · เข้าคิวรออนุมัติตามปกติ (ครอบด้วย R14-DB-04) | ✅ |
| R14-FIX-03 | ยิง API พร้อม `siteId` ของโครงการที่ไม่ได้ดูแล | ยังถูกปฏิเสธเหมือนเดิม (R14-DB-05) — เปิดให้ "ไม่เลือก" ไม่ใช่เปิดให้ "เลือกของคนอื่น" | ✅ |
| R14-FIX-04 | คิวอนุมัติของเจ้าของ | ชิปโครงการขึ้น **เฉพาะใบเก่าที่เคยผูกไว้** · ใบใหม่ไม่มีชิป ไม่ใช่ขึ้นว่า "ไม่ระบุโครงการ" ทุกใบ | 👤 |
| R14-FIX-05 | หน้าแรกของเจ้าของ เมื่อมีคนติดลบ | แถบ "คนงานเบิกเกินค่าแรงค้างจ่าย N คน · รวม ฿X" กดไป `/payroll` ได้ · ไม่มีใครติดลบ = ไม่วาดแถบ | 👤 |
| R14-FIX-06 | `/payroll` เมื่อมีคนติดลบ | การ์ดยอด "เบิกเกินค้างอยู่" สีแดงพร้อมจำนวนคน (ไม่มีใครติดลบ = ไม่วาดการ์ด) | 👤 |
| R14-FIX-07 | `/attendance` ของ **เจ้าของ** | ป้าย "เบิกเกิน ฿X" ข้างชื่อคนทั้งในลิสต์เลือกคนและแถวที่ลงชื่อแล้ว | 👤 |
| R14-FIX-08 | `/attendance` ของ **หัวหน้าโครงการ** | ไม่มีป้ายใด ๆ และ **ตัวเลขไม่เดินทางมาถึงเบราว์เซอร์เลย** (page ไม่ยิง query ให้เขา · R14-DB-28 เป็นตาข่ายรอง) | 👤 |
| R14-FIX-09 | ยอดติดลบหลังลงชื่อ | ลดลงทันทีตามค่าแรงที่เกิด โดยใบเบิกยังไม่ถูกหัก (R14-DB-26) | ✅ |
| R14-FIX-10 | ร่องรอยการหักคืน | `audit_log` ของ `advances` มี before/after ของ `deducted_amount` ทุกครั้ง (R14-DB-27) | ✅ |
